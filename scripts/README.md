# Scripts

`check_trail.py` verifies a trail JSON file.

`search_22_integer_box.py` runs an exploratory, time-limited search for a 22-link candidate.

Both scripts use only the Python standard library.
