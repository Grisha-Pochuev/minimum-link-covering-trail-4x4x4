# Step 3 — smart-search-25-core-valley

Prepared from the binding Step-2 handoff.

- Full search loop is C++20.
- Search state is realized as finite contact spans, not only an infinite-line graph.
- Every primary mutation removes two supporting lines and adds two lines atomically.
- The first half-move is never scored or pruned separately.
- Permanent inputs are verified by bundle hash, inner hashes, row counts, and exact coverage.
- Preflight exercises all 20 modes and representative dual verification.
- Smoke is strict 20/20 and gates the full 20-shard matrix.
- Full profile is 20 shards × 4 workers × 20400 seconds, timeout 359 minutes.
- Periodic checkpoints are written every 600 seconds.
- Every saved finite trail is checked by two independent exact verifiers.

Local web-chat validation used a reduced bundle containing all 43 plateau states:

- all 20 modes executed;
- strict miniature 20/20 aggregate succeeded;
- best control remained exactly 62/64;
- minimum common-17 overlap reached 15;
- C++ throughput was roughly 2,400–12,600 atomic attempts/s in the miniature probe.

The long run must not be interpreted as a proof of impossibility if it finds no 63/64 trail.
