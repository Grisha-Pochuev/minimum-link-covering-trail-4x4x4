# Compact representatives for run 28674416173

Workflow: `smart-search-16-defect-relay-60`

Result: no new reusable compact representative was added.

The aggregation artifact reported:

- relay `60+` rows: `60`
- reported unique compact `60+`: `2`
- unique missing families: `1`

Important interpretation: `build_defect_relay_summary.py` scanned all JSON/JSONL files with `covered_count`, including `missing_patterns_shard_*.json` metadata. The `relay60-diversity.jsonl` artifact contains one full candidate row with `vertices2` and one metadata row without geometry. The full candidate row is the same geometry as the previous recorded frontier candidate `mlct22-3cf45a2e21fe611c` from run `28618565146`.

Reusable compact bank additions from this run: `0`.

Full best candidate is saved in `best_candidate.json` for run provenance, but it should not be merged as a new compact reusable curve.
